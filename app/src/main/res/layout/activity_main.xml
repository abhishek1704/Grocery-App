<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#FFFFFF"
    tools:context=".MainActivity">

    <EditText
        android:id="@+id/nameTextView"
        android:layout_width="309dp"
        android:layout_height="wrap_content"
        android:layout_alignParentStart="true"
        android:layout_alignParentTop="true"
        android:layout_centerHorizontal="true"
        android:layout_marginStart="58dp"
        android:layout_marginTop="76dp"
        android:ems="10"
        android:hint="Item "
        android:inputType="textPersonName"
        android:textSize="24sp" />

    <TextView
        android:id="@+id/amntTextView"
        android:layout_width="40dp"
        android:layout_height="56dp"
        android:layout_below="@id/nameTextView"
        android:layout_centerHorizontal="true"
        android:layout_margin="10dp"
        android:text="0"
        android:textAlignment="center"
        android:textSize="35sp" />

    <Button
        android:id="@+id/decBtn"
        android:layout_width="60dp"
        android:layout_height="wrap_content"
        android:layout_alignTop="@id/amntTextView"
        android:layout_alignBottom="@id/amntTextView"
        android:layout_toStartOf="@id/amntTextView"
        android:text="-"
        android:textSize="18sp" />

    <Button
        android:id="@+id/incBtn"
        android:layout_width="60dp"
        android:layout_height="wrap_content"
        android:layout_alignTop="@id/amntTextView"
        android:layout_alignBottom="@id/amntTextView"
        android:layout_toEndOf="@id/amntTextView"
        android:text="+"
        android:textSize="18sp" />

    <Button
        android:id="@+id/addBtn"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignTop="@id/incBtn"
        android:layout_alignBottom="@id/incBtn"
        android:layout_toEndOf="@id/incBtn"
        android:text="Add"
        android:textSize="20sp" />

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/recyclerView"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_below="@id/amntTextView"
        android:background="#FFFFFF"></androidx.recyclerview.widget.RecyclerView>

</RelativeLayout>